{
   "name": "bmad-method",
   "build": {
      "dockerfile": "Dockerfile"
   },
   "remoteUser": "vscode",
   "remoteEnv": {
      "GITHUB_TOKEN": "${localEnv:GITHUB_TOKEN}"
   },
   "postCreateCommand": "npx bmad-method install --yes --modules bmm,bmb,cis --tools claude-code",
   "customizations": {
      "vscode": {
         "extensions": [
            "anthropic.claude-code",
            "swiftlang.swift-vscode"
         ]
      }
   }
}