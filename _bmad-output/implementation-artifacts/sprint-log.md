# Sprint Log

Full narrative history of `sprint-status.yaml`'s `last_updated` field, archived here 2026-08-06 to fix the "unbounded ever-growing single line" problem flagged in `deferred-work.md` across three separate reviews (2-14, 3-1, 3-2). Going forward, `sprint-status.yaml`'s `last_updated` field holds only the current entry; each time it's overwritten, the previous entry is appended below before being replaced.

## 2026-08-08 (Story 3.7 code-review: no application code changed by this story (audit-and-test story, no .swift diff) — code-review agent confirmed there was nothing to review beyond sprint-tracking metadata. Zero findings. Status moved to done.)

## 2026-08-08 (Story 3.5 code review: accessibility fix confirmed, story done)

3-5-end-to-end-accessibility-validation: code review patched a misleading accessibilityStateValue on decided-but-not-chosen ChoiceCardView cards (was announcing "Not yet selected."); fixed with a new storyChoice.choiceCard.state.notChosen value/key. User-verified via Accessibility Inspector re-run — fix confirmed working. Status moved to done. Four smaller findings deferred to deferred-work.md.

## 2026-08-07 (Sprint Change Proposal: VoiceOver dropped from official v1 scope)

3-5-end-to-end-accessibility-validation / cross-cutting: user decided, mid-Story-3.5, to drop official VoiceOver testing/support for v1 — discovered VoiceOver does not run in the iOS Simulator at all (physical device required, confirmed via Apple's own developer forums), and no physical-device testing workflow is available. Every other accessibility requirement (Dynamic Type NFR8, Reduce Motion NFR5, WCAG AA/1.4.11 contrast NFR7, 44pt tap targets NFR6, and FR-11's "every gesture has a standard tap equivalent" rule, confirmed kept despite VoiceOver being its original motivation) stays fully in scope, unaffected. Ran `bmad-correct-course`: batch-mode Sprint Change Proposal (`sprint-change-proposal-2026-08-07.md`) approved and implemented — PRD FR-11, epics.md (FR11, UX-DR12, Story 3.5's ACs 1/6/8 rewritten from live-VoiceOver checks to code/Accessibility-Inspector-based structural checks, Story 4.2/4.4's VoiceOver-authoring ACs reworded/dropped), EXPERIENCE.md's Accessibility Floor, project-context.md (new Process Agreements entry), Story 3.5's file, and deferred-work.md (3 annotations) all updated. No `.swift` files touched. Net effect: Story 3.5's remaining Simulator checklist shrinks and no longer needs VoiceOver or a physical device at all (Accessibility Inspector Audit pass, Dynamic Type AX5, button clearance, EndingView swipe-back, Reduce Motion). Status held at in-progress pending that checklist.

## 2026-08-07 (3-5 dev-story: code-verifiable audit complete, Simulator checklist handed to user)

3-5-end-to-end-accessibility-validation: story file created via create-story workflow — closing accessibility/contrast audit for Epic 3, sequenced after Story 3.6 per that story's own note. Closes four deferred-work.md items: Story 2.2's VoiceOver rotor no-op audit, Story 2.8's runOptionsButtonClearance AX5 re-verification, Story 3.4's EndingView backSwipeGesture-at-accessibility-size reachability check, and Story 3.4's WCAG 1.4.11 non-text contrast pairs (pre-computed reference values included: ink-primary border and trace-brass divider both clear 3:1 in both themes). No engine-logic changes anticipated; entire story is manual Xcode/Simulator verification, unrunnable in this devcontainer. Status ready-for-dev

## 2026-08-07 (3-6 done, 3-5 story created)

3-6-reading-and-ending-surface-background-and-frame-inset-rule: code review complete and user-re-verified in Xcode/Simulator — status done. Review applied 3 patches post-Task-4-verification, then 4 further rounds resolving the accessibility-size scroll geometry the patches disturbed. Root cause of that whole chain: the inline GeometryReader in readingComposition/EndingView.content reports a proxy.size that is ALREADY safe-area-reduced while proxy.safeAreaInsets still reports the device's insets — every round that subtracted them double-counted. Final shape: View.accessibilitySizeFramedScroll() does no safe-area math at all, insetting the scroll viewport by Spacing.large to match readingCardPadding's horizontal margin. Also: duplicated FrameView color computation factored into currentColor. swift test 89/89 clean.

## 2026-08-06 (deferred-work.md review entry)

deferred-work.md review: closed out every remaining open item in the deferred-work log — see that file and epics.md for details. New stories added: 3-6-reading-and-ending-surface-background-and-frame-inset-rule, 3-7-run-progress-refresh-on-dismiss-test-coverage-and-consolidation-audit, 3-8-memory-and-tutorial-polish-and-deferred-item-cleanup. Amended stories: 3-5-end-to-end-accessibility-validation (added AX5 run-options clearance, VoiceOver no-op audit, EndingView swipe-back-at-accessibility-size re-verification, non-text WCAG 1.4.11 contrast pairs), 4-2-full-prose-authoring (added Tutorial VoiceOver rotor-action copy AC), 4-3-story-tree-wiring (added 5 testing/decision notes for gaps only resolvable once the real content tree exists). Also fixed the sprint-status.yaml last_updated unbounded-growth problem itself by archiving full history through this date to sprint-log.md — see that file for everything predating this entry, and its own header note for the new convention going forward.

## 2026-08-06 and earlier (archived in full)

3-6-reading-and-ending-surface-background-and-frame-inset-rule: new story added to Epic 3 backlog via deferred-work review — bundles the two [TRACKED — needs a new story] gaps from the 3.4 code review (surface-base/surface-raised background never applied across Reading/Ending/Memory; frame's inset-rule border never drawn), see epics.md Story 3.6 and deferred-work.md's 3-4 entry for full context; sequenced before 3-5 in this file (though numbered after it) so the closing accessibility/contrast pass validates the corrected screens; status backlog; earlier same-day: 3-4-ending-and-memory-visual-identity: user re-verified the 3 code-review patches (EndingView.swift's captionStyle() continueHint, MemoryView.swift's per-row .accessibilityElement(children: .combine) + divider .accessibilityHidden) in Simulator — confirmed both look good; status advanced review -> done per project-context.md's new Process Agreement requiring fresh verification after post-review patches; earlier same-day: 3-4-ending-and-memory-visual-identity: code review complete — independent re-audit (3 parallel review layers: Blind Hunter, Edge Case Hunter, Acceptance Auditor) found the story's original "already fully compliant, zero gaps" audit claim didn't fully hold; 3 decision-needed items resolved (2 deferred to a new story — ending-frame/memory-row's background:{colors.surface-raised} token and the frame's inset-rule border are both entirely unimplemented anywhere in the codebase since Story 2.5, spanning Reading too not just Ending/Memory, so tracked as a follow-up rather than patched narrowly here; 1 patched — EndingView's continueHint used SwiftUI's built-in .caption instead of DESIGN.md's own same-named typography.caption token, added Typography.swift's new captionStyle() and applied it), 3 patches applied (captionStyle() token fix above; MemoryView recap rows gained .accessibilityElement(children: .combine) per row, matching EndingView's own established VoiceOver-fragmentation fix; MemoryView's inter-row divider gained .accessibilityHidden(true) matching FrameView's corner-mark precedent), 3 items deferred (EndingView swipe-back possibly unreachable at accessibility Dynamic Type sizes due to a ScrollView/gesture race, Memory's "+0" neutral-score formatting, non-text WCAG 1.4.11 contrast pairs not covered by DESIGN.md's text-contrast table), several findings dismissed as unreachable/non-issues (MemoryView divider-index edge case verified structurally unreachable via StoryRunEngine's selectChoice guard + RunSnapshot.loadValid, ForEach duplicate-identity risk, button-style reuse, inert FrameView animation on Ending's static isActive); swift test 89/89 passing post-patches; status left at review (not done) pending user's fresh Xcode/Simulator re-verification of the 3 patched files, since the story's existing manual verification predates these patches — new process rule added to project-context.md's Process Agreements: a code review that patches real .swift code after a story's manual verification already happened must not advance status to done on the strength of the pre-patch verification; earlier same-day: 3-4-ending-and-memory-visual-identity: implemented via dev-story workflow — audit-only pass (Tasks 1-3) re-verified EndingView.swift/MemoryView.swift/FrameView.swift/Typography.swift/ButtonStyles.swift token-by-token against DESIGN.md's ending-frame/memory-row/memory-score definitions and found the implementation already fully compliant, zero code changes made (no inline/raw colors, all text color pairs covered by DESIGN.md's Verified contrast table including the Memory-score large-text accent-ember pair named by example there); swift test 89/89 passing (no code touched); Task 4 manual Xcode/Simulator verification confirmed by user 2026-08-06 (light/dark appearance contrast on both screens, accessibility Dynamic Type without truncation, VoiceOver reads/activates all three actions with comfortable tap targets); no deferred-work.md entries needed (audit surfaced no out-of-scope gaps); status review; earlier same-day: 3-4-ending-and-memory-visual-identity: story file created via create-story workflow — audited EndingView.swift/MemoryView.swift/FrameView.swift/Typography.swift directly against DESIGN.md's ending-frame/memory-row/memory-score tokens and confirmed all three were already wired during Stories 3.2/3.3 (both prior stories implemented against these exact token names in their own scope); framed this story as a targeted audit + gap-fix + formal contrast/accessibility verification pass rather than a from-scratch build, with an explicit warning against speculative rewrites of already-compliant code; status ready-for-dev; earlier same-day: 3-3-memory-recap-screen: code review complete — 3 patches applied (trailing divider after Memory's last recap row removed, MemoryView.chosenOption(for:) now reuses StoryRunEngine.option(withId:in:) instead of duplicating it, stale EndingView File List entry corrected), 1 item deferred (AC #8's "generated symbols" wording, pre-existing project-wide gap vs. project-context.md's actual plain-dot-path-key rule), 8 findings dismissed (unguarded startNewRun() by design, dated narrative comments matching established project convention, EndingView's asymmetric swipe/tap by deliberate user-confirmed decision, accessibility .combine/.accessibilityAction split confirmed working, ForEach id:\.self duplicate-identity risk foreclosed by an existing engine guard, placeholder copy state matching convention, TierTests' coupling to placeholder boundaries, and the EmptyView()-replacing-preconditionFailure fix already tracked/resolved within this diff's own deferred-work.md update); swift test 89/89 passing post-patches; status done; earlier same-day: 3-3-memory-recap-screen: Task 7 manual Xcode/Simulator verification confirmed by user across several rounds — all 4 endings reach Memory with correct choice/consequence row text/score sign/tier label per path (including AC #7's single-row .gotcha hard-fail case), Return Home and Start New Run both work, holds at accessibility Dynamic Type and landscape, VoiceOver reads score/tier/rows sensibly with both action buttons reachable; along the way, user-driven testing surfaced and fixed a real chain of EndingView bugs unrelated to Memory itself: (1) a preconditionFailure crash on every reading-to-ending transition with Reduce Motion off, caused by an animated-transition rendering race re-invoking a fading-out readingComposition's body against the live engine after currentNodeId had already changed — fixed by rendering EmptyView() instead of crashing; (2) a misdiagnosed "ScrollView-missing overflow" clipping report that was actually HeadlineTextStyle's .tracking() breaking Text wrapping into a single truncated line at ordinary Dynamic Type sizes — fixed with .fixedSize(horizontal: false, vertical: true); (3) swipe-back on Ending (an intentional pre-Epic-3 product decision, since a committed choice stays committed regardless of navigating back over it) got silently dropped by Story 3.2's dedicated gesture wiring and was further obscured by the (ultimately unnecessary) ScrollView fix attempted for bug 2 — reverted the ScrollView back to Story 2.8's accessibility-size-conditional pattern and added a real backSwipeGesture; (4) an Accessibility Inspector Audit finding ("Element has no description") caused by that same temporary ScrollView addition breaking .accessibilityElement(children: .combine)'s ability to walk down into a ScrollView-wrapped Text tree — fixed by moving .combine directly onto the VStack containing the Text children; project-context.md extended in three places (gesture-arbitration's wrapping-vs-overflow misdiagnosis lesson, Dynamic Type & Accessibility's tracking-vs-wrapping and .combine-vs-ScrollView placement rules, Navigation's EndingView-swipe-back-is-intentional note) so future stories don't rediscover any of this live; swift test 89/89 passing throughout (76 prior + 13 new: 9 Tier boundary cases, 4 StoryRunEngine cases); status review; earlier same-day: 3-3-memory-recap-screen: Tasks 1-6 implemented via dev-story workflow — non-defaulted consequenceKey field added to ChoiceOption, new Tier/scoreToTier(score:) pure function in Engine/Tier.swift per AD-9, new StoryRunEngine.startNewRun() intent, new .statStyle()/.metaStyle() typography modifiers, the real MemoryView created and wired into StoryChoiceView replacing the Story-3.2-era placeholder, deferred-work.md's Return-Home-affordance gap and Memory-placeholder entries resolved, 13 new Swift Testing cases added; status in-progress pending Task 7; earlier same-day: 3-3-memory-recap-screen: story file created via create-story workflow — adds a non-defaulted `consequenceKey` field to `ChoiceOption` (mirrors `EndingPayload.titleKey`/`bodyKey`'s Story 3.2 precedent), a new `Tier`/`scoreToTier(score:)` pure function in `Engine/Tier.swift` per AD-9 (deliberately Engine-layer not Content-layer, since it derives from a runtime `Int`, not an authored node — see Dev Notes), a new `StoryRunEngine.startNewRun()` intent (AD-3 already named it; `startFreshRunIfCurrentRunHasEnded()` refactored to delegate to it), and the real `MemoryView` (new `Views/Memory/` directory, no circuit frame per DESIGN.md, rows scroll unconditionally unlike Reading/Ending since Memory has no competing swipe gesture to dodge) wired into `StoryChoiceView` replacing the Story-3.2-era "coming in Story 3.3" placeholder; explicitly closes the Return-Home-affordance gap `deferred-work.md` tracked against this story from Story 3.2's code review; scopes out a dynamic per-run title (no content-model support, no AC requires it) same shape as Story 3.2's illustration cut; status ready-for-dev; earlier same-day: 3-2-ending-screen: code review complete — 1 decision-needed resolved (no exit-to-Home affordance on Ending/Memory once a run ends, confirmed regression from the pre-diff placeholder which had a working RunOptionsButton; user decision: accept as an interim dead-end for this merge, but tracked in deferred-work.md as a required explicit AC for Story 3.3, not an implicit side effect), 5 patches applied (goBack() now resets hasAdvancedPastEnding so a back-then-forward sequence through an ending node no longer reports stale .memory; FrameView.swift's stale "never wraps the Ending placeholder" doc comment corrected; EndingView gained .accessibilityElement(children: .combine) so its custom accessibility action reliably surfaces; Memory placeholder's dead RunOptionsButton-clearance padding removed; the new snapshot-absence test switched from object(forKey:) to .data(forKey:) matching every other test in the suite), 3 items deferred (preconditionFailure crash-risk-on-future-drift, invented endingElsewhere/endingLimbo placeholder copy, sprint-status.yaml's own ever-growing last_updated field), swift test 76/76 passing post-patches, status done; earlier same-day: 3-2-ending-screen: Task 6 manual Xcode/Simulator verification confirmed by user — build, swift test, and all Simulator checks (4 endings' text/eyebrow/permanent Frame, tap-anywhere advance, accessibility Dynamic Type, VoiceOver read + accessible advance action, force-quit/relaunch fresh-install Home state) all successful; all 6 tasks complete; status review; earlier same-day: 3-2-ending-screen: Tasks 1-5 implemented via dev-story workflow — EndingPayload gained non-defaulted titleKey/bodyKey (all 4 StoryTree.swift call sites updated with placeholder-quality copy, .endingHomeward/.endingHardFail matching mockups/ending.html verbatim), a Views-layer-only EndingKind-to-eyebrow-key mapping added, the real EndingView.swift created and wired into StoryChoiceView.body as a dedicated top-level phase branch (not routed through readingComposition, per the story's own Dev Notes reasoning), StoryRunEngine gained Phase.memory + a hasAdvancedPastEnding flag so advancePage() from Ending has somewhere to go (rendering only a temporary "coming in Story 3.3" placeholder, same shape as the Epic-2-era Ending placeholder this story itself replaced), and 4 new Swift Testing cases added; swift test 76/76 passing (72 prior + 4 new), 3 consecutive clean runs, 0 flakes; Task 6 (manual Xcode/Simulator verification) awaits the user — devcontainer has no Xcode/Simulator, actively requested per project-context.md's Process Agreement rather than left as a silent gap; status in-progress (not review) until that verification lands, matching Story 2.13's precedent; earlier same-day: 3-2-ending-screen: story file created via create-story workflow — replaces StoryChoiceView's Epic-2 Ending placeholder with a real EndingView (new Views/Ending/ directory), adds titleKey/bodyKey to EndingPayload (non-defaulted, mirroring Story 3.1's kind field), an EndingKind-derived eyebrow label (no content-model change needed, unlike Story 2.8's eyebrow-tag exclusion for Story/Choice), and a new engine Phase.memory + advancePage() transition so "tap anywhere" has somewhere to go — rendering only a temporary "coming in Story 3.3" placeholder there, same shape as the Epic-2-era Ending placeholder this story itself replaces; explicitly scoped text-only, no illustration (checked mockups/ending.html directly — no image element exists there despite epics.md AC1's "text/illustration" wording); status ready-for-dev; earlier same-day: 3-1-ending-kind-resolution: code review complete, 4 patches applied (EndingKind made Equatable + new test asserting each terminal node's EndingPayload.kind matches what StoryTree.swift authors, closing the gap where only NodeID reachability was tested not kind correctness; .driftLimbo's alignmentDelta corrected from 0 to 2 per Task 3's own non-zero requirement; the bundled 3-scenario onlyTheGotchaChoiceReachesTheHardFailEnding test split into 3 independent tests; Dev Notes' "no UI impact" framing corrected — firstChoice now renders 4 choice cards via the existing StoryChoiceView/ChoiceCardView, and a manual Xcode/Simulator check was requested but not yet performed), 1 item deferred (sprint-status.yaml's own ever-growing last_updated field, pre-existing, already tracked from the 2-14 review), 4 findings dismissed (ARCHITECTURE-SPINE.md traceability-table phrasing nuance, ending-node-mapping comment duplicated across 3 spots, placeholder gotcha-choice copy not reading as an obvious trap, self-reported flake-freedom claim); swift test 72/72 passing post-patches (69 prior + 3 net from the split/new tests), 3 consecutive clean runs; user confirmed Xcode/Simulator verification 2026-08-05 (build + swift test succeeded, Simulator shows all 4 firstChoice cards, selecting either new option correctly navigates to the Ending placeholder) — status done; earlier same-day: implemented via dev-story workflow — EndingKind (home/stay/limbo/hardFail) added as a non-optional field on EndingPayload (Content/StoryNode.swift), two new terminal nodes (.endingLimbo, .endingHardFail) and two new firstChoice options (the designated gotcha hard-fail choice, and a limbo-reaching choice) wired into StoryTree.swift, two new Localizable.xcstrings entries added, 4 new Swift Testing cases added to StoryRunEngineTests.swift (immediate hard-fail transition, hard-fail-only-via-gotcha reachability, snapshot-clearing on both new endings); confirmed AC #2/#3/#7 required zero engine code changes (StoryRunEngine's existing generic .ending-phase derivation, selectChoice(_:) synchronous assignment, and persistOrClearSnapshot()'s .ending branch already covered them) — this story's real work was content-tree wiring plus new traceability tests; swift test 69/69 passing (65 pre-existing + 4 new), 3 consecutive clean runs, 0 flakes; no Xcode/Simulator step needed (engine/content-only, no UI — that's Story 3.2); status review; earlier same-day: story file created via create-story workflow — adds EndingKind (home/stay/limbo/hardFail) as a non-optional field on EndingPayload so every terminal node's authoring site is a compile error until it supplies a kind (AD-1/AD-6), extends the placeholder tree with two new terminal nodes (.endingLimbo, .endingHardFail) and two new firstChoice options (a designated gotcha choice and a limbo-reaching choice) to exercise all four EndingKind cases, and adds Swift Testing coverage for the immediate-selectChoice-to-Ending transition and hard-fail-only-via-gotcha reachability; noted in Dev Notes that both the Ending-clears-snapshot behavior (AC #7) and the immediate-phase-transition behavior (AC #3) are already generically implemented in StoryRunEngine and need only new tests, not new engine code; status ready-for-dev; epic-2-retrospective: completed — 14/14 stories reviewed, key insight: SwiftUI gesture arbitration (DragGesture vs ScrollView vs interactivePopGestureRecognizer) was the single biggest recurring bug source across Stories 2.2/2.3/2.8/2.9, now documented in project-context.md's new "SwiftUI gesture arbitration" section; devcontainer-vs-real-device verification gap also recurred (Stories 2.4/2.7/2.8), Process Agreements extended accordingly; 3 action items recorded (2 done immediately - doc updates, 1 open - flag gesture/nav-architecture stories as higher-risk at story-creation time); Epic 3 confirmed ready, no epic update needed; full retro saved to epic-2-retro-2026-08-05.md; status done; earlier, 2026-08-04: 2-14-fix-flaky-tests-under-swift-testings-parallel-execution: code review complete, 2 patches applied (UserDefaultsTestMutex.lock() made cancellation-aware via withTaskCancellationHandler so a cancelled waiter is removed and rethrows instead of hanging indefinitely; AC #4's stale "64 pre-existing tests" corrected to 65), 1 item deferred (sprint-status.yaml's ever-growing last_updated field, pre-existing), 10 findings dismissed (speculative future-code risks, refuted claims, or established project conventions), 6 consecutive swift test runs post-patch, 65/65 passing each time, status done; earlier same-day: all 4 tasks implemented via dev-story workflow — root-caused the flakiness to swift-corelibs-foundation's CFPreferences holding its domain cache and per-application-name preferences cache as process-global state shared across every UserDefaults(suiteName:) value rather than per-suite state, confirmed with a minimal reproduction outside this codebase; fixed with a custom Swift Testing trait (SerializesUserDefaultsAccess, TestSupport.swift) backed by a continuation-based async mutex, applied only to the three suites that touch UserDefaults (StoryRunEngineTests, RunSnapshotTests, RunSnapshotPresenceTests) so the two that don't (ForkedEchoesTests, RunOptionsButtonTests) keep running in parallel; freshDefaults() and all existing test assertions unchanged; verified with 20 consecutive swift test runs, 20/20 passed, 0 flakes (suite is 65 tests, not the 64 estimated when the story was drafted — RunOptionsButtonTests' 1 test, added during 2.13's code review pass, wasn't counted); status review; 2-13-run-options-sheet-exit-and-clear-progress: Task 6 manual verification confirmed by user — Xcode build, swift test, and Simulator check of AC #7 all successful (fresh-install Home state, clean new run, correct VoiceOver announcements, portrait and landscape), all tasks complete, status review; 2-14-fix-flaky-tests-under-swift-testings-parallel-execution: new bug story added to Epic 2 backlog, surfaced during 2-13's own swift test verification — a write-then-immediate-read race on freshly-minted UserDefaults(suiteName:) instances under Swift Testing's parallel execution, affecting observerRefreshPicksUpASnapshotWrittenAfterConstruction/anEngineResumedOntoShoreArrivalWithoutDismissalStillReportsInterstitialPhase/anEngineResumedOntoANonEchoNodeReportsIsEchoActiveFalseImmediately (none touched by 2.13, confirmed pre-existing), test-infrastructure-only scope, status ready-for-dev; 2-13-run-options-sheet-exit-and-clear-progress: Tasks 1-5 implemented via dev-story workflow — StoryRunEngine.exitAndClearProgress() added, RunOptionsRow ordered-data shape added (new Engine file) and wired into RunOptionsButton's options sheet, third confirmation dialog + onExitAndClearProgress closure added and wired at StoryChoiceView's call site, two runOptions.* localization keys added, four new Swift Testing cases added, ARCHITECTURE-SPINE.md AD-3 amended, swift test 64/64 passing; Task 6 (manual Simulator verification) awaits the user; status in-progress; earlier same-day: story file created via create-story workflow — adds a fourth "Exit and Clear Progress" sheet row + confirmation, a new StoryRunEngine.exitAndClearProgress() intent (resets state like restartRun() but clears rather than re-persists the on-disk RunSnapshot, so Home lands in its fresh-install "Start Story" state, not "Resume Story" — the key design decision flagged in the story's Dev Notes), two new runOptions.* localization keys, and a testable ordered-data structure backing an automated row-order assertion (closing the gap Story 2.7's code review flagged), status ready-for-dev; 2-12-run-options-sheet-fix-popover-presentation-and-missing-cancel: code review complete, 3 patches applied (gated the .cancel-role removal behind #available(iOS 26, *) so the app's supported iOS 18-25 range keeps native Cancel styling/VoiceOver trait — the popover auto-suppression only exists on iOS 26+; corrected sprint-status.yaml's stale "pending verification" narrative; added sprint-status.yaml to the story's File List), 8 findings dismissed (unverifiable-but-tool-sourced citations, no UI test framework exists by convention, AC scope narrowing was the user's explicit decision, comment length matches repo style, review-not-done status is expected mid-review, struck-through AC history matches doc convention), swift test 60/60 passing, status done; earlier same-day: first fix attempt (.presentationCompactAdaptation(.none), horizontal-size-class hypothesis) disproven by user-provided post-fix Simulator screenshot; re-investigated via WebSearch/WebFetch against Apple WWDC 2025 Session 284 and confirmed real cause: iOS 26 intentionally changed confirmationDialog/actionSheet to anchor to its triggering button on iPhone (matching iPadOS), no supported way to force the old bottom-sheet style for a non-nav-bar-anchored control; user decided to accept this as the new platform-native presentation (UX-DR11 amended in epics.md) and narrow the fix to the genuinely-fixable missing-Cancel-row defect, resolved by dropping the .cancel role from both Cancel buttons in RunOptionsButton.swift (popover-style auto-suppression targets the .cancel role specifically, documented UIAlertController behavior since iOS 8); swift test 60/60 passing; second round of manual Simulator verification confirmed by user 2026-08-04 (both dialogs show all three rows including a visible, functional Cancel; Cancel and tap-outside both dismiss cleanly; verified portrait and landscape), all tasks complete, status review; earlier same-day: story file created via create-story workflow, status ready-for-dev; 2-11-tutorial-navigation-and-fixed-actions-layout: code review complete - Acceptance Auditor found zero AC violations, 1 patch applied (stale NavigationStack-level .environment(engine) in RootView.swift removed, left over from Story 2.7's Tutorial retrofit), 11 findings dismissed (spec-intended behavior or verified non-issues), manual Simulator verification confirmed by user 2026-08-04, status done; earlier same-day: "Back Home" button and its localization key removed, layout restructured so "Start Story"/"Resume Story" sits outside the scrollable region as a fixed sibling of the ScrollView, run-options overlay and its now-unused engine/dismiss environment values removed from TutorialView.swift, Home and RunOptionsButton.swift confirmed untouched by diff review, swift test 60/60 passing; 2-7-run-options-action-sheet: code review complete, all patches applied (Tutorial restart guard, UX-DR12 focus-traversal order, runOptions.* localization key rename), status done; 2-9-branch-arrival-interstitial-first-visit-only-gate: code review complete, all patches applied, status done; 2-10-persist-back-navigation-across-app-relaunch: code review complete, 1 decision-needed resolved (AC #3 composition test accepted given placeholder content-tree limitation) + 3 patches applied (duplicate-entry round-trip test, on-disk reload assertion, interstitial-gate-with-resumed-stack test), 5 items deferred (all low-severity, pre-existing patterns or tampered-data-only reachability), status done; 2-8-reading-surface-visual-identity-dynamic-type-and-reduce-motion: code review complete, 3 decision-needed resolved + 7 patches applied (Reduce Motion choice-card touch-handling bug — 2 rounds, full-bleed interstitial padding regression, page-turn transition, interstitial caption uppercase, caption-bar-accent accessibility, headlineStyle() color refactor, choice-card arrow glyph), 6 items deferred, manual Simulator verification confirmed by user 2026-08-03, status done; 2-12-run-options-sheet-fix-popover-presentation-and-missing-cancel: new bug story added to Epic 2 backlog, surfaced via user-reviewed Simulator screenshot during a Sally/UX discussion of run-options open items — run-options confirmationDialog rendering as an anchored popover with no Cancel row instead of the spec'd bottom action sheet; 2-13-run-options-sheet-exit-and-clear-progress: new story added to Epic 2 backlog, resolves the second deferred-work.md/epic-2 open design question — sheet gains a fourth "Exit and Clear Progress" option, closes the "no single-tap way to clear progress and land on Home" gap; the interstitial-control open question was discussed and closed with no story needed, per UX-DR11's 2026-08-03 addendum; 2-11-tutorial-navigation-and-fixed-actions-layout: story file created via create-story workflow, status ready-for-dev

## 2026-08-08

2026-08-08 (Story 3.7 story file created via create-story workflow — audit-and-test story for Home/Tutorial's run-progress refresh wiring, closing the Story 2.7 code-review deferred item. Story file's own research already found the audit conclusion documented in RootView.swift's existing comments (two genuinely distinct triggers: fullScreenCover dismissal vs. NavigationStack push/pop) and the AC #3 Swift Testing case already existing in RunSnapshotPresenceTests.swift (added Story 2.4 code review) — dev agent's job is largely to confirm and document both, not redo them. Status ready-for-dev.)

## 2026-08-08

2026-08-08 (Story 3.7 dev-story: audit + test-coverage confirmation complete. Task 1/AC #1 re-traced both refresh triggers via actual call sites (not just the existing code comment) and confirmed they cover genuinely disjoint SwiftUI lifecycle events — fullScreenCover dismissal (RootView's .onChange) vs. NavigationStack push/pop (Home/TutorialView's .onAppear). Task 2/AC #2: no redundancy found, no code change. Task 3/AC #3: confirmed RunSnapshotPresenceTests.swift's existing observerRefreshPicksUpASnapshotWrittenAfterConstruction() (from Story 2.4's code review) fully satisfies AC #3; swift test 89/89 passed. No .swift files changed this story. Status held at in-progress pending Task 4's manual Xcode/Simulator verification from the user.)

## 2026-08-08

2026-08-08 (Story 3.7 dev-story: Task 4 manual Xcode/Simulator verification completed by user, all three checks (a/b/c) passed with no staleness found. All tasks complete, all ACs satisfied, 89/89 tests passing, no .swift files changed (audit confirmed both refresh triggers are genuinely necessary, no redundancy; existing test already satisfied AC #3). Status moved to review.)
